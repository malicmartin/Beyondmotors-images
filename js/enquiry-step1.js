function selectMotor(card){// Remove 'selected' class from all cards const cards=document.querySelectorAll('.enquiry-card');cards.forEach(c=>c.classList.remove('selected'));// Add 'selected' class to the clicked card card.classList.add('selected');// Update the hidden input field with the selected value document.getElementById('enquiry_motor_model').value=card.getAttribute('data-value')}// Prevent form submission if no card is selected document.getElementById('enquiry-form').onsubmit=function(event){const selectedMotor=document.getElementById('enquiry_motor_model').value;if (!selectedMotor){alert("Please select a motor model before proceeding.");event.preventDefault()}};
